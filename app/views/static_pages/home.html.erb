<div class="center hero-unit">
  <h1>Welcome to the Movie_DB</h1>
  <% if !signed_in? %>
  	<%= link_to "Sign up now!", signup_path, class: "btn  btn-primary" %> to give reviews or <%= link_to "sign in", signin_path %> if you already have an account
  <% end %>
</div>
<%= render 'movies/all_movies' %>


